# Core dependencies
underthesea
dateparser
tkcalendar
ics
pyinstaller
babel
python-docx==1.1.0
matplotlib
openpyxl
reportlab

# UI dependencies
customtkinter>=5.2.0
pillow

# Audio playback
playsound==1.2.2

# PhoBERT dependencies (optional but recommended)
# Install with: pip install -r requirements.txt
# For CPU: torch torchvision torchaudio
# For GPU (CUDA 11.8): torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
torch>=2.0.0
transformers>=4.30.0
tqdm>=4.65.0
scikit-learn>=1.3.0
